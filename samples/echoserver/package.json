{
  "name": "botium-connector-grpc-sample",
  "version": "0.0.1",
  "scripts": {
    "start": "node index.js",
    "docker:build": "docker build -t botium/grpc-sample .",
    "docker:push": "docker push botium/grpc-sample",
    "docker:run": "docker run --restart always -p 50051:50051 -d botium/grpc-sample"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.5.5",
    "grpc": "^1.24.3"
  }
}
